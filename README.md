# Kiki's Kitchen Table

Kiki’s Kitchen Table is a simple shopping page backed by a local content management tool.

The content is managed locally in a Docker-based tool where items can be added, edited, and updated, then exported to a static `index.html` page for the public site.

The basic idea is straightforward: keep the source of truth local, then publish plain static output.
